{
  "buildCommand": "pip install -r requirements.txt",
  "functions": {
    "api/wsgi.py": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/wsgi"
    }
  ]
}
